- If you create a constructor method to build a DVD by title then create a constructor method to build
a DVD by category. Does JAVA allow you to do this?
Java does not allow this because both constructors have the same method signature. 
They have the same number of parameters, the same parameter types, and the same order. 
This leads to a duplicate constructor error.